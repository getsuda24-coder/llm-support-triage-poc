# App
APP_ENV=local
DB_PATH=/app/data/app.db

# LLM
LLM_PROVIDER=mock   # mock | openai
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Routing thresholds (simple heuristic router)
AUTO_SEND_MAX_RISK=0.30

# RAG
RAG_TOP_K=4
RAG_MAX_CHARS=1400

# Agent loop
AGENT_MAX_TURNS=4
